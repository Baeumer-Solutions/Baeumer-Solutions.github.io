{
  "name": "p12-web",
  "version": "1.0.0",
  "description": "Protect-12 Website (statisch, Git, CMS-getrieben)",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "serve": "node build.mjs && npx --yes serve dist"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^12.0.0"
  }
}
